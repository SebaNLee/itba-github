int
main(void)
{
	int i;
	i = 9;
	return 0;
}

// se linkedita con el flag -c, devuelve un archivo tipo .o, diferente al .out que es el ejecutable

// pude compilarlo y no linkeditarlo al sacarle el flag -Wall que me decía sobre el no uso del var i

// después de cambiarle el permiso con chmod, no me deja ejecutarlo por no ser binario