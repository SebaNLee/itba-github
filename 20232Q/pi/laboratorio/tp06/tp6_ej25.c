


unsigned int copiaSubVector(const char *arregloIn, char *arregloOut, unsigned desde, unsigned hasta, unsigned dimMax){

    if(dimMax <= 0 || hasta > desde || desde < 0){
        return 0;
    }

    int dimAux, i; 

    for(i = desde, dimAux = 0; i < hasta || dimMax < dimMax; i++, dimAux){





    }

    arregloOut[i] = 0;

    return dimAux + 1;



}

// ??






