# include <stdio.h>

int
main(void)
{
    int i, j, max;
    i = j = 2;
    max = (i>j ? i : j);

    return 0;
}

/* I a i, es case sensitive
** end a return y se le agrega ;
** y el operador condicional ? tiene que tener un espacio por en medio
*/ 

